# Elicit Survey — Requirements

> Generated by the AIUP `/requirements` skill from `docs/vision.md`, cross-checked against the
> already-reverse-engineered `docs/use_cases/UC-001..006.md` and `docs/entity_model.md` so this
> catalog matches what the codebase actually does today, not just the original intent. Each
> functional requirement below traces to the use case it was derived from — keep that mapping
> when either document changes (see the Working Agreements in `CLAUDE.md`).

## Functional Requirements

| ID     | Title                          | User Story                                                                                                                                     | Use Case | Priority | Status      |
|--------|---------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------|----------|----------|-------------|
| FR-001 | Token-Based Login               | As a respondent, I want to log in with the token I was given so that I can access my assigned survey without creating an account.                | UC-001   | High     | Implemented |
| FR-002 | Survey Selection                | As a respondent, I want to select which survey my token belongs to when more than one survey is configured so that I reach the correct survey.  | UC-001   | Medium   | Implemented |
| FR-003 | Auto-Registration for Testing   | As a developer or tester, I want an unrecognized token to auto-create a respondent when auto-registration is enabled so that I can exercise the survey flow without pre-provisioned data. | UC-001   | Low      | Implemented |
| FR-004 | Answer Survey Questions         | As a respondent, I want to answer each question with my input validated against its configured rules so that my responses are captured correctly. | UC-002   | High     | Implemented |
| FR-005 | Adaptive Branching               | As a respondent, I want the survey to show, hide, or repeat downstream questions, sections, or steps based on my prior answers so that I only see questions relevant to me. | UC-002   | High     | Implemented |
| FR-006 | Repeating Questions              | As a respondent, I want a question to repeat as many times as a prior numeric answer indicates so that I can give one response per repeated item (e.g., per sibling). | UC-002   | Medium   | Implemented |
| FR-007 | Navigate Between Sections        | As a respondent, I want to move forward and backward between survey sections so that I can revise earlier answers before finishing later ones.   | UC-002   | Medium   | Implemented |
| FR-008 | Review Answers Summary          | As a respondent, I want to see a read-only summary of everything I've answered, grouped by section, before submitting so that I can confirm accuracy. | UC-003   | High     | Implemented |
| FR-009 | Finalize Survey                  | As a respondent, I want to submit my completed survey so that my responses are locked in and made available for reporting.                       | UC-004   | High     | Implemented |
| FR-010 | ETL Extraction on Finalize        | As a downstream reporting consumer, I want a respondent's completed section path extracted into analytical tables when they finalize so that I can analyze outcomes across respondents. | UC-004   | High     | Implemented |
| FR-011 | Post-Survey Action Notifications | As an integration operator, I want the system to call each configured post-survey webhook when a respondent finalizes so that dependent downstream processes are triggered. | UC-004   | Medium   | Implemented |
| FR-012 | View Configured Reports          | As a respondent, I want to see the report(s) configured for my survey after finalizing so that I understand the outcome of my responses.         | UC-005   | High     | Implemented |
| FR-013 | Download PDF Summary             | As a respondent, I want to download a PDF of my submitted answers so that I have a personal record of what I reported.                           | UC-005   | High     | Implemented |
| FR-014 | Post-Survey Redirect             | As a respondent, I want to be sent to a configured external URL after viewing my reports (when one is configured) so that I can continue to any follow-up destination. | UC-005   | Low      | Implemented |
| FR-015 | Log Out                          | As a respondent, I want to log out and clear my session so that my survey data isn't accessible to the next person using this device.            | UC-006   | Medium   | Implemented |

## Non-Functional Requirements

| ID      | Title                              | Requirement                                                                                                                    | Category        | Priority | Status      |
|---------|-------------------------------------|----------------------------------------------------------------------------------------------------------------------------------|------------------|----------|-------------|
| NFR-001 | Accessible Rendering                | The respondent-facing UI must conform to WCAG 2.1 AA, achieved through Vaadin's built-in accessible components rather than raw HTML. | Usability        | High     | Implemented |
| NFR-002 | Operational Observability           | Login, answer-save, and finalize operations must emit OpenTelemetry traces and metrics sufficient to diagnose failures without server log access. | Maintainability  | Medium   | Implemented |
| NFR-003 | PDF Availability Window             | A generated PDF must remain retrievable through its cache link for at least 10 minutes after generation.                          | Availability     | Medium   | Implemented |
| NFR-004 | Data-in-Transit Protection          | All respondent answer data, including PHI in clinical deployments, must be transmitted using TLS 1.2 or higher.                    | Security         | High     | Open        |
| NFR-005 | Container Portability               | The system must build and run as a single, self-contained Docker image deployable without host-specific dependencies.             | Portability      | High     | Implemented |
| NFR-006 | Session Resilience                  | On a transient browser disconnect/reconnect, the system must restore the respondent's in-progress session state without requiring re-authentication, for the life of the underlying HTTP session. | Availability     | Medium   | Implemented |
| NFR-007 | Unguessable Report Artifact Keys    | Any publicly reachable, unauthenticated download link (e.g., the PDF cache key) must use a cryptographically random identifier, not one derived from predictable inputs such as timestamps. | Security         | High     | In Progress |

## Constraints

| ID    | Title                            | Constraint                                                                                                                                          | Category    | Priority | Status      |
|-------|-----------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------|-------------|----------|-------------|
| C-001 | License                          | The project must be licensed under PolyForm Noncommercial 1.0.0.                                                                                     | Regulatory  | High     | Implemented |
| C-002 | Runtime Platform                 | Backend must run on Java 25.                                                                                                                          | Technical   | High     | Implemented |
| C-003 | Application Framework            | Must use Quarkus 3.34.x.                                                                                                                              | Technical   | High     | Implemented |
| C-004 | UI Framework                     | Must use Vaadin 25.1.x Flow (server-side UI); no client-side rewrite (e.g., Hilla/React) without explicit approval.                                  | Technical   | High     | Implemented |
| C-005 | Data Access Layer                | Must use Hibernate ORM with Panache (JPA); jOOQ-specific patterns are prohibited.                                                                     | Technical   | High     | Implemented |
| C-006 | Database Platform                | Must use PostgreSQL.                                                                                                                                   | Technical   | High     | Implemented |
| C-007 | Build & Deployment               | Must build with Maven and deploy via Docker.                                                                                                          | Technical   | High     | Implemented |
| C-008 | Shared Schema Ownership          | The database schema and message contracts are shared with the companion Admin and Authoring modules; schema changes must remain compatible across all three. | Operational | High     | Implemented |
| C-009 | Scope Boundary                   | Must not implement survey authoring, subject registration/token issuance, or cross-survey analytics dashboards — these remain the responsibility of the Authoring tool, Admin app, and downstream reporting tools respectively. | Business    | Medium   | Implemented |
| C-010 | PHI Handling Obligation          | Deployments such as the Family Health History Survey may process PHI; respondent answer data must be handled as potentially sensitive under applicable regulatory obligations. | Regulatory  | High     | Open        |
| C-011 | Accessible Component Usage       | Vaadin components must be used in a way that preserves the framework's built-in WCAG support; regenerated or refactored code must not bypass accessible component APIs with raw HTML. | Technical   | Medium   | Implemented |

## Known Gaps Affecting These Requirements

These are implementation defects uncovered during reverse-engineering, not requirements changes.
They're tracked in `docs/plans/fix-review-findings.md` and are called out here so a status of
"Implemented" above isn't read as "defect-free":

- **FR-005 / NFR-007:** The PDF cache key backing NFR-007 is currently a predictable
  `time+nanoTime` string, not a cryptographically random value — hence "In Progress," with the
  fix and a matching update to UC-005's BR-013 tracked in the plan.
- **FR-005:** The `MODAL` question type is unimplemented (placeholder rendering only), and the
  "less than" branching operator is implemented identically to "greater than" — both latent
  defects in adaptive branching, not intended behavior (see UC-002 Notes/Known Gaps).
- **FR-008:** The Review page's Finish button reuses the Previous button's enabled/disabled
  condition, which is very likely a copy-paste defect (see UC-003 Notes/Known Gaps).
- **FR-011:** No post-survey action ever records a terminal "succeeded" status — success is only
  inferred from the absence of a recorded failure (see UC-004 BR-011).
- **NFR-004 / C-010:** TLS-in-transit and PHI-handling posture depend on how this application is
  deployed (reverse proxy/ingress termination, hosting environment) and cannot be verified from
  the application codebase alone — marked "Open" pending confirmation against the actual
  deployment configuration.
